class file {
    constructor(title, date, text, template) {
        this.title = title,
            this.date = date,
            this.text = text,
            this.template = template
    }
}
module.exports = file;